# AWS Lambda

This folder contains the AWS Lambda entry point for the project.

Responsibilities:

- Extract GitHub data
- Upload raw JSON to Amazon S3 Bronze

It intentionally does not execute the Silver or Gold pipelines.